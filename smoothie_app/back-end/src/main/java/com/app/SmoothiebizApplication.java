package com.app;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Start point for Smoothie Shop application
 * In order to start the application just run this class as Java application
 * @author Kamlesh
 *
 */
@SpringBootApplication
public class SmoothiebizApplication {

	/**
	 * This is the main method to load the application
	 * @param args
	 */
	public static void main(String[] args) {
		SpringApplication.run(SmoothiebizApplication.class, args);
	}
}
